# Tests: the standalone Redis started by EmbeddedRedis (port 6399).
# The test profile is activated by the test task in build.gradle.
spring.data.redis.host=localhost
spring.data.redis.port=6399
wannasong.data-dir=build/test-data
